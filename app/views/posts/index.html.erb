<h1>質問一覧</h1>
<% @questions.each do |question| %>
<% student=Student.find_by(id: question.student_id)%>
<img src="<%= "/user_images/#{student.image_name}" %>">
<%= link_to(question.content, "/posts/#{question.id}") %>
<p></p>
<%end%>
